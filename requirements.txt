# Core (PyTorch install osobno z --index-url dla CUDA)
# pip3 install torch --index-url https://download.pytorch.org/whl/cu129
numpy

# Data loading (HuggingFace)
datasets
tqdm

# Tokenizer fallback (when Rust unitokenizer not available)
tokenizers

# Logging & Visualization
wandb
tensorboard

# Optional
torchvision
torchviz
requests
